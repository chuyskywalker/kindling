
<form id="authy" method="post" action="/auth/log<?= $this->hasAuth ? 'out' : 'in' ?>">
<p>
    &copy; <?
    $s = date('Y', $this->startdate);
    $e = date('Y', $this->enddate);

    echo ($s == $e) ? $s : "$s - $e";

    ?> <?= defined('AUTHOR') ? AUTHOR : 'Anonymous' ?>
    -
        <input type="submit" value="Log<?= $this->hasAuth ? 'out' : 'in' ?>" />
</p>
</form>
